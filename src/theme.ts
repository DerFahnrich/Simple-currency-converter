import { createTheme } from "@mui/material";

export const theme = createTheme({
  palette: {
    background: { default: "#23272a", paper: "#23272a" },
    mode: "dark",
    primary: {
      main: "#7289da",
    },
  },
});
